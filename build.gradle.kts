plugins {
    id (Plugins.application) version (Plugins.version) apply false
    id (Plugins.library) version (Plugins.version) apply false
    id (Kotlin.plugin) version (Kotlin.version) apply false
    id (DaggerHilt.plugin) version (DaggerHilt.version) apply false
}